# archive

Nothing here is live. It is the drawer for things kept rather than deleted.

- `screenshots/` — loose captures that used to sit in the repo root. Still gitignored by
  `*.png`, so the folder is empty for anyone who clones; it exists to keep the root readable
  on the working machine.

Nothing in the app reads this folder. Nothing here is tracked except this file.
